/**
 * Placeholder for screen 2a (Home).
 *
 * This exists so `lint` and `build` have real code to check from the first
 * commit onward. The real screen is built once the token layer, primitives and
 * layout chrome are in place — see the plan's Phase 1 and Phase 3 Lane D.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Smoke Show Labs</h1>
      <p>Wholesale storefront — under construction.</p>
    </main>
  )
}
